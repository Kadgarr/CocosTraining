export enum ColorType {
    WHITE,
    BLACK
}

export enum TrackSide {
    BOTTOM, // Нижняя сторона (движение слева направо)
    RIGHT,  // Правая сторона (движение снизу вверх)
    TOP,    // Верхняя сторона (движение справа налево)
    LEFT    // Левая сторона (движение сверху вниз)
}



